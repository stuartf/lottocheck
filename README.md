Put the numbers you play into the `picks` file space separated with the
mega ball as the last number on each line.

Then when you run lottocheck.py it will dump out an html page showing which
numbers matched.
